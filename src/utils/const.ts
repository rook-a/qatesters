export const enum NameSpace {
  Order = 'Order',
}

export const enum FetchStatus {
  Idle = 'Idle',
  Pending = 'Pending',
  Fulfilled = 'Fulfilled',
  Rejected = 'Rejected',
}

export const enum AppRoute {
  Root = '/',
  NotFound = '*',
}
